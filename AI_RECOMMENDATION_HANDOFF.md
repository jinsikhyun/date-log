# date.log AI 추천 기능 인계서

> 목적: 이 문서는 date.log의 **AI 추천 기능에 관해 사용자와 확정한 내용만** 정리한다.  
> 다른 기능의 할 일이나 일반적인 개선 아이디어는 포함하지 않는다.  
> 작성일: 2026-09-03

## 1. 제품 목표

date.log에 쌓인 장소 정보와 사용자가 선택한 태그를 바탕으로, 실제 존재하는 장소 중 커플의 취향과 현재 맥락에 잘 맞는 곳을 추천한다.

AI가 장소를 지어내거나 단독으로 검색하는 구조가 아니다.

1. **Kakao Local API**가 실제 장소 후보를 찾는다.
2. **OpenAI API**가 현재 장소·코스·태그·기존 기록을 분석한다.
3. AI는 카카오 후보를 재정렬하고 추천 이유를 작성한다.
4. 최종 카드에는 장소명, 카테고리, 주소, 거리, 추천 이유, 관련 태그, Kakao Map 링크를 표시한다.

## 2. 확정된 추천 개수

- 최초 결과는 **3개**를 기본으로 한다.
- 사용자가 더 보고 싶을 때 **2개를 추가**할 수 있다.
- 한 번에 최대 **5개**까지만 보여준다.
- 10개 추천은 과도하므로 사용하지 않는다.

## 3. 장소 상세 페이지 추천

- 위치: 장소 상세 페이지의 **`근처 다른 곳 보기` 아래**.
- 현재 보고 있는 장소의 다음 정보를 추천 맥락으로 사용한다.
  - 이름
  - 카테고리
  - 주소 및 좌표
  - 한줄평·설명
  - 사용자가 선택한 태그
  - 커플의 기존 장소 및 선호 기록
- 거리 제한은 기본적으로 강제하지 않는다.
- 단, date.log의 사용 범위가 서울·경기 중심이므로 **서울·경기 후보를 기본 검색 범위로 제한**해 생뚱맞은 지방 추천과 API 낭비를 막는다.
- 가까운 곳을 우선하는 선택지를 제공할 수 있지만 장소 상세 추천에서는 기본 강제 조건이 아니다.

## 4. 데이트 코스 작성 중 추천

- 코스에 장소가 **1개 들어간 시점부터** AI 추천을 사용할 수 있다.
- 장소가 1개일 때는 해당 장소와 자연스럽게 이어지는 후보를 추천한다.
- 장소가 2개 이상이면 공통 분위기·카테고리 조합·동선을 분석해 추천 감도를 높인다.
- 코스 추천에서는 **가까운 곳 우선**이 기본이다.
- 이미 코스에 들어간 장소와 date.log에 중복된 장소는 적절히 제외한다.
- 추천 장소를 누르면 코스 순서에 추가할 수 있어야 한다.

## 5. 지역 및 거리 정책

- 전국을 무제한 검색하지 않는다.
- 1차 후보는 서울·경기로 제한한다.
- 장소 상세:
  - 동일·유사 콘셉트가 중요하다.
  - 거리는 점수 요소로 사용할 수 있지만 기본 하드 컷은 두지 않는다.
- 데이트 코스:
  - 동선이 중요하므로 선택된 장소들의 좌표를 기준으로 가까운 후보를 우선한다.
  - 코스가 늘어날수록 마지막 장소 또는 코스 중심 좌표 등을 기준으로 거리 점수를 반영한다.
- 추천 이유에는 사용자가 이해할 수 있는 거리·동선 근거를 짧게 포함한다.

## 6. 태그 UX 및 추천 입력

태그는 추천 정확도를 높이지만 사용자에게 의무나 숙제처럼 느껴지면 안 된다.

### UX 원칙

- Naver 지도 리뷰 키워드처럼 **보고 바로 누를 수 있는 칩 UI**를 사용한다.
- 입력 화면에는 상황에 맞는 추천 태그 **6~8개를 먼저 노출**한다.
- `더보기`에서 전체 태그를 그룹별로 보여준다.
- 기존 목록에 없는 태그를 사용자가 직접 추가할 수 있다.
- 태그 선택은 선택 사항이다.
- 권장 선택 수는 **3~5개**, 최대 **8개** 정도로 제한한다.
- 장소의 카테고리와 입력 맥락에 따라 앞에 보이는 태그를 다르게 구성한다.

### 확정 태그 체계

#### 분위기

- 조용한
- 감성적인
- 편안한
- 고급스러운
- 이색적인

#### 목적

- 대화하기 좋은
- 특별한 날
- 사진 찍기 좋은
- 일상 데이트
- 오래 머물기 좋은

#### 공간

- 전망 좋은
- 아늑한
- 넓은
- 전시형 공간
- 야외 좌석

#### 음식·메뉴

- 커피·음료 중심
- 디저트
- 술
- 가벼운 식사
- 든든한 식사
- 매운 음식

#### 시간·상황

- 낮 데이트
- 저녁 데이트
- 야경
- 비 오는 날
- 주말

### 데이터 구조 방향

- MVP는 `places.tags text[]` 형태를 우선 검토한다.
- 사용자 정의 태그도 같은 배열에 저장할 수 있다.
- 태그 테이블 정규화는 검색·통계 요구가 커진 뒤 검토한다.
- 실제 마이그레이션은 아직 작성하거나 운영 DB에 적용하지 않았다.

## 7. AI 모델 및 보안

- 비용을 아끼기 위해 소형·저비용 모델을 사용한다는 방향을 확정했다.
- 논의 중 선택한 모델 명칭은 **Luna**였지만, 구현 시 사용자의 OpenAI API 계정에서 실제 사용 가능한 정확한 API 모델 ID와 가격을 공식 문서로 다시 확인한다.
- 사용자는 OpenAI API에 **$7를 충전**한 상태이므로 MVP는 짧은 입력과 구조화된 출력으로 비용을 통제한다.
- OpenAI API 키는 절대로 `NEXT_PUBLIC_...` 환경변수에 넣지 않는다.
- 키는 Vercel 서버 환경변수에만 저장하고 Next.js 서버 Route Handler에서 호출한다.
- 브라우저에서 OpenAI API를 직접 호출하지 않는다.
- 응답은 자유 텍스트가 아니라 구조화된 JSON으로 받는다.

권장 응답 필드:

```ts
type AiRecommendation = {
  kakaoPlaceId: string;
  name: string;
  category: string;
  address: string;
  lat: number;
  lng: number;
  distanceMeters: number | null;
  reason: string;
  matchedTags: string[];
  kakaoMapUrl: string;
};
```

## 8. 대표 이미지 정책

Kakao Local API는 장소 대표 이미지를 제공하지 않는다. Google Places Photos는 별도 결제·장소 재매칭·출처 표시·캐시 정책이 필요하므로 MVP에서는 사용하지 않는다.

확정 우선순위:

1. 추천 장소가 이미 date.log에 있고 대표사진이 있으면 해당 사진을 사용한다.
2. 사진이 없으면 카테고리 이모지와 기존 크림 스트라이프 배경을 사용한다.
3. 이모지 이미지 영역을 누르면 **네이버 이미지 검색**을 새 탭으로 연다.
4. 카드 하단에도 `사진 보기 ↗` 링크를 둔다.
5. 검색어는 `장소명 + 주소의 구/동` 조합으로 만들어 동명의 다른 지점이 섞이는 문제를 줄인다.
6. AI 생성 이미지는 실제 장소 사진처럼 오해될 수 있으므로 사용하지 않는다.

이미 구현된 검색 URL 함수:

- `src/lib/places.ts`의 `naverImageSearchUrl(name, address)`

## 9. 확정된 AI 추천 카드 UI

사용자가 현재 미리보기 UI에 만족한다고 확인했다. 이후 구현은 이 디자인을 기준으로 유지한다.

### 카드 구성

- 상단 대표 이미지 또는 카테고리 이모지 플레이스홀더
- 좌측 상단 카테고리 칩
- 우측 상단 `AI 추천` 칩
- 장소명과 거리
- 주소
- 1~2문장의 추천 이유
- 일치 태그 최대 3개
- `사진 보기 ↗`
- `Kakao Map`
- 데스크톱 3열, 모바일 1열
- 기존 Archive Teal 팔레트, 둥근 카드, 넉넉한 여백 유지

### 구현 파일

- 재사용 카드: `src/components/AiRecommendationCard.tsx`
- 확정 UI 미리보기와 고정 더미 데이터: `src/components/AiRecommendationPreview.tsx`
- 개발 전용 QA 페이지: `src/app/auth/ai-recommend-qa/page.tsx`

로컬 미리보기:

```text
http://localhost:3000/auth/ai-recommend-qa
```

- QA 페이지는 `NODE_ENV !== "development"`에서 `notFound()` 처리하므로 운영 배포에서 노출되지 않는다.
- 미리보기 첫 번째 카드의 `/brand-app-icon.png`는 **기존 사진이 있는 상태를 검증하기 위한 임시 fixture**일 뿐, 실제 추천 이미지가 아니다.
- 실제 연결 시 `imageUrl`에는 date.log의 일치 장소 `places.image_url`만 전달한다.

## 10. 현재 구현 상태

완료:

- AI 추천 카드 UI 컴포넌트
- 사진 있음/없음 표현 분기
- 카테고리 이모지 fallback
- 장소명·주소 기반 네이버 이미지 검색 링크
- 추천 이유·태그·거리·Kakao Map 표시
- 데스크톱 및 모바일 UI 확인
- 새 AI UI 파일만 대상으로 한 ESLint 통과
- 태그 선택 UI 컴포넌트(`TagSelector`)와 개발 전용 QA 미리보기(`/auth/tag-select-qa`)
  - 태그 사전·카테고리별 추천 우선순위: `src/lib/tags.ts` (§6 확정 태그 체계 그대로 반영, DB 컬럼 없이 정적 상수)
  - 재사용 컴포넌트: `src/components/TagSelector.tsx`
  - QA 미리보기(카테고리 스위치로 추천 태그 변화 확인용): `src/components/TagSelectorPreview.tsx`
  - 브라우저로 직접 확인: 카테고리 전환 시 추천 태그 6~8개 재구성, 칩 선택/해제, `더보기` 그룹 전체 노출, 직접 추가(Enter/버튼), 최대 8개 도달 시 나머지 칩·입력 비활성화까지 정상 동작 확인
  - `npx eslint`, `npx tsc --noEmit` 신규 파일 기준 통과 (`datelog.design/` 폴더의 기존 타입 오류는 이번 작업과 무관한 별도 미설치 의존성 문제)
- `places.tags text[]` 컬럼 마이그레이션(`supabase/add-place-tags.sql`) — 사용자가 Supabase SQL Editor에서 직접 실행·검증
- 장소 추가/수정 폼(`AddPlaceForm.tsx`, 다녀온 곳·위시리스트·수정 공용)에 `TagSelector` 연결, 저장 시 `places.tags`에 반영
- 장소 상세 페이지(`PlaceDetail.tsx`)에 저장된 태그 칩 표시
- `places` 조회 3곳(HomeView/WishlistView/PlaceDetail) select 컬럼에 `tags` 추가, 컬럼 누락 시 안내 메시지 처리
- OpenAI SDK(`openai`, `zod`) 설치, `OPENAI_API_KEY` 서버 전용 환경변수 등록(`.env.local`, `.env.example`)
- 추천 Route Handler `src/app/api/ai-recommend/route.ts` — 로그인 인증, Structured Outputs, 서버측 거리 계산까지 실제 호출로 검증 완료 (아래 §11 5번 참고)
- `KAKAO_REST_API_KEY` 서버 전용 환경변수 등록, `src/lib/kakaoLocal.ts` + `src/app/api/kakao-candidates/route.ts` — 서울·경기 필터·기존 장소 중복 제외까지 실제 검색으로 검증 완료 (아래 §11 6번 참고)
- 장소 상세 페이지 AI 추천 UI 실제 연결(`AiRecommendationSection.tsx`) — 저장된 실제 장소로 끝까지(카카오 검색 → AI 재정렬 → 카드 표시 → 더보기) 검증 완료 (아래 §11 8번 참고)
- 후보 수집·AI 입력 구조 개선(자기 자신/같은 주소/50m 이내 제외, 다중 검색어, 정확도·거리 혼합, 가중치 기반 평가) — §8.1 참고
- 코스 작성 화면 AI 추천 연결(`CourseForm.tsx`, `mode: "course"`) — 코스가 1개 이상일 때부터, 마지막 정거장 기준 거리 최우선 + 코스 전체 컨셉 반영, "+ 코스에 추가"까지 실제 검증 완료 (아래 §11 9번 참고)
- 비용 제한(커플당 시간당 20회 — 처음엔 인메모리였다가 이후 DB 기반으로 교체, 아래 참고) + 오류/빈 상태/최대 5개/새로고침 검증 (아래 §11 10번 참고)

- 추천 시 matchedTags를 §6 확정 태그 체계(+ 그 요청에 실제로 쓰인 place/코스 태그)로 제약 — Zod `z.enum()`으로 스키마 자체를 매 요청 동적으로 만들어 구조적으로 강제(단순 프롬프트 지시가 아님). 실제 호출로 확인: "인근", "식사 후" 같은 자유 문구 대신 "조용한", "커피·음료 중심" 등 확정 태그만 반환됨.
- 장소 상세 페이지 추천 카드에 "+ 위시리스트" 버튼 추가(`AiRecommendationCard.tsx`에 옵션 prop, QA 미리보기는 영향 없음). 클릭 시 `status: wishlist`로 저장, 성공하면 "추가됨"으로 바뀌고 중복 클릭 방지. 실제 클릭으로 검증: "춘천막국수"가 위시리스트에 정상 추가되고 상세 페이지에도 뜸.
- **추천 피드백 → 취향 내로잉**: "+ 위시리스트"/"+ 코스에 추가"로 저장할 때 AI가 그 후보를 고른 근거(`matchedTags`)를 그 장소의 `places.tags`로 그대로 저장한다(별도 피드백 테이블 없이 기존 `tags` 컬럼 재사용). `/api/ai-recommend`는 매 호출마다 커플이 저장한 모든 장소의 `tags` 빈도를 집계해 상위 8개를 `coupleFavoriteTags`로 프롬프트에 포함하고, "취향·태그·분위기 유사도"(장소 상세) / "코스 전체 컨셉"(코스) 기준에 반영하도록 지시한다. 즉 AI 추천을 받아들일수록(위시리스트/코스에 추가할수록) 다음 추천이 그 취향 쪽으로 좁혀진다.
  - 순수 로직 테스트(네트워크 없음): 태그 빈도 집계·정렬·상위 N개 제한·빈 배열/null 안전 처리 — 3개 케이스 통과.
  - 실제 클릭으로 검증: AI 추천 "커피그래"를 위시리스트에 추가 → 상세 페이지에 태그 칩 "커피·음료 중심" 정상 표시(다음 추천부터 `coupleFavoriteTags` 후보에 들어감).
  - **버그 발견 및 수정**: 이 작업 도중 `MAX_OUTPUT_TOKENS=800`이 너무 낮아 OpenAI 응답이 중간에 잘려(`Unterminated string in JSON`) "추천 응답을 이해하지 못했어요" 오류가 실제로 재현됨 — `gpt-5.6-luna`가 보이는 JSON 앞에 내부적으로 토큰을 더 쓰는 것으로 추정. `2000`으로 올려 해결(출력 단가가 1M 토큰당 $1.20라 비용 영향은 미미). 재현 후 정상 동작 재확인함.
- **비용 제한 DB화**: `supabase/add-ai-recommend-rate-limit.sql` — `ai_recommend_calls(couple_id, created_at)` 테이블 추가, 기존 `my_couple_id()`/`set_couple_id()`(add-couple-rls.sql) 재사용해 커플 스코프 RLS 적용. `/api/ai-recommend`가 최근 1시간 자기 커플 행 수를 세어 20개 이상이면 429. DB 오류(마이그레이션 미적용 등)면 막지 않고 통과(가용성 우선) — 실제로 마이그레이션 적용 전 상태로 호출해서 정상 통과되는 것 확인(로그에 PGRST205 "table not found"만 찍히고 기능은 안 깨짐). 사용자가 SQL 실행함.
- **commit, push, Vercel 배포 완료** (2026-09-03). 커밋 `0846eca`, `origin/main` push. GitHub 연동으로 자동 재배포됨. 배포 직후엔 `KAKAO_REST_API_KEY`/`OPENAI_API_KEY`를 push 이후에 Vercel에 등록해서 "장소 후보 검색이 아직 설정되지 않았어요" 에러가 떴음 — Vercel은 이미 만들어진 배포에 환경변수를 소급 적용하지 않으므로 대시보드에서 수동 Redeploy 필요했음(원인 파악 + 안내). 재배포 후 사용자가 실제 프로덕션(`datelog.kr`)에서 로그인해 AI 추천 정상 동작 확인함.

아직 하지 않음:

- `add-ai-recommend-rate-limit.sql`의 429가 실제로 걸리는지까지는 확인 안 함 (시간당 20회를 일부러 다 채워야 해서 보류 — 사용자가 "자명하니 넘어가자"고 판단)

## 11. Claude가 이어서 작업할 순서

사용자가 각 UI 단계에서 미리보기를 보고 피드백하는 방식을 원한다. 한 번에 전체 기능을 완성하거나 배포하지 말고 아래 순서대로 진행한다.

1. 현재 QA UI와 이 문서를 읽고 확정 디자인을 유지한다. — 완료
2. 태그 선택 UI를 별도 개발 전용 미리보기로 만든다. — 완료 (`/auth/tag-select-qa`)
3. 사용자에게 태그 UI 검증을 받는다. — 완료
4. 승인 후 `places.tags` 마이그레이션과 장소 추가·수정 폼을 연결한다. — 완료. `supabase/add-place-tags.sql` 작성 후 사용자가 직접 Supabase SQL Editor에서 실행·검증함(Claude는 실행하지 않음). 장소 추가/수정 폼에 `TagSelector` 연결, 장소 상세에 저장된 태그 표시.
5. OpenAI 키가 서버 전용인지 확인하고 추천 Route Handler를 만든다. — 완료.
   - `openai`, `zod` 패키지 설치. 모델은 `gpt-5.6-luna` (OpenAI 공식 모델 페이지 확인: 입력 $0.20 / 출력 $1.20, 1M 토큰당 — GPT-5.6 세대 저비용 티어. 사용자가 ChatGPT에서 들었던 "Luna"가 실제 모델명이었음, gpt-5-mini보다 저렴).
   - `src/app/api/ai-recommend/route.ts`: POST, 로그인 사용자만 호출 가능(비로그인은 `src/proxy.ts` 전역 미들웨어가 먼저 `/login`으로 리다이렉트, 라우트 자체에도 `auth.getUser()` 이중 체크). `OPENAI_API_KEY`는 `.env.local`/Vercel 서버 환경변수에만 있고 `NEXT_PUBLIC_` 아님 — 브라우저 번들 미노출.
   - Structured Outputs(zod 스키마, Responses API)로 JSON 강제. AI 응답 id가 candidates 목록에 없으면 서버에서 버림(장소 조작 방지). 거리(distanceMeters)는 AI가 아니라 서버가 `src/lib/courses.ts`의 `haversineKm`으로 직접 계산.
   - 후보 20개/출력 800토큰/결과 최대 5개로 상한.
   - 로그인된 브라우저에서 더미 후보 3개로 실제 호출 검증: 200 응답, 구조화된 JSON 정상 수신, 거리 계산 정상, 비로그인 요청은 로그인 페이지로 리다이렉트됨 확인.
   - 관찰된 이슈(추후 7단계에서 다듬을 것): (1) 한 번은 reason 문장에 비한국어 단어가 한 개 섞여 나옴 — 저비용 모델의 드문 흔들림으로 추정, 재시도 시 정상. (2) matchedTags 를 §6 확정 태그 체계로 제한하지 않아 AI가 임의 문구를 태그처럼 반환함 — 실제 UI 연결 전(7~8단계)에 허용 태그 목록으로 제약 필요.
6. Kakao Local API에서 실제 후보를 수집하고 서울·경기 범위 및 중복 필터를 적용한다. — 완료.
   - `KAKAO_REST_API_KEY`(REST API 키, JS 키와 다름) 서버 전용 환경변수 등록.
   - `src/lib/kakaoLocal.ts`: 카카오 로컬 키워드검색(`/v2/local/search/keyword.json`) 래퍼. 반경(최대 20km)·거리순 정렬·최대 3페이지(45개) 수집, 서울/경기 밖 주소 제외(`isSeoulGyeonggi`), `kakao_map_link`에서 place id 추출(`extractKakaoId`).
   - `src/app/api/kakao-candidates/route.ts`: POST, 로그인 필요. `query/lat/lng` 기준 검색 후 커플이 date.log에 이미 저장한 장소(`places.kakao_map_link` 기반)와 요청의 `excludeKakaoIds`를 제외하고 반환.
   - 로그인된 브라우저에서 실제 검색 검증: 강남역 인근 "카페" 검색 → 서울 주소만 정상 반환. 프랜차이즈 카테고리가 브랜드명("공차")으로 나오는 문제 발견 후 중분류를 쓰도록 수정(`shortCategory`), 재검증 완료. 이미 저장된 장소가 있는 지역에서 같은 이름으로 재검색해 결과에서 빠지는 것도 확인(중복 필터 동작).
   - 참고: Kakao 키워드검색은 이름에 검색어가 들어가면 실제 카테고리가 달라도 걸릴 수 있음(예: "카페" 검색에 인쇄소 "프린트카페"가 포함됨) — 이런 노이즈는 필터링하지 않고 그대로 후보에 남겨둠. 7단계에서 AI가 candidate의 실제 category 필드를 보고 걸러내는 방식으로 처리.
7. AI에 후보 목록만 전달하여 3개를 재정렬하고 추천 이유와 태그를 구조화된 JSON으로 받는다. — `/api/ai-recommend`(5단계)가 이미 이 역할을 함. 남은 일은 `/api/kakao-candidates`(6단계) 결과를 그 입력으로 연결하는 것뿐 — 8단계(장소 상세 UI 연결)에서 함께 진행.
8. 장소 상세의 `근처 다른 곳 보기` 아래에 실제 추천 UI를 연결한다. — 완료.
   - `src/components/AiRecommendationSection.tsx`: `PlaceDetail.tsx`에서 `NearbySimilar` 바로 아래 렌더링. 기본 접힘 — 처음 펼칠 때만 `/api/kakao-candidates` → `/api/ai-recommend` 순서로 호출(유료 API 는 명시적 클릭 시에만 실행).
   - 한 번의 AI 호출로 5개까지 받아두고, 처음엔 3개만 보여주다가 "더보기"는 **새 API 호출 없이** 숨겨둔 2개를 그대로 드러냄(비용 절약, §2 "기본 3개 + 더보기 2개, 최대 5개" 그대로 구현). "다시 추천받기"는 전체 재호출.
   - 카드는 확정 UI(`AiRecommendationCard`) 그대로 재사용 — 새로 발견한 곳이라 `imageUrl`은 항상 null(이모지+네이버 이미지 검색 링크로 대체).
   - 실제 저장된 장소("코오모라멘")에서 끝까지 검증: 펼치기 → 로딩 → 실제 근처 술집/카페/분식 3곳이 이유·태그·거리와 함께 표시 → "더보기"로 2곳 추가(로딩 없이 즉시) → 카드 디자인·데스크톱 레이아웃 정상.

### 8.1 사용자 피드백 반영 — 거리 위주 저품질 추천 개선 (2026-09-03)

초기 버전이 "같은 건물 0m 포장마차, 26m 분식, 32m 카페"처럼 거리만 가까운 저품질 후보를 추천한다는 피드백을 받고, 후보 수집·AI 입력 구조·프롬프트를 함께 고쳤다.

**후보 수집 (`src/lib/kakaoLocal.ts`, `src/app/api/kakao-candidates/route.ts`)**

- `category` 하나만이 아니라 `category` + `tags`(최대 2개) 조합으로 여러 검색어를 만들어 각각 조회(`collectCandidates`).
- 검색어마다 `accuracy`(정확도순)·`distance`(거리순) 둘 다 조회해 섞는다 — 거리순 결과만 쓰지 않음.
- 모든 조합 결과를 Kakao place id 기준으로 중복 제거.
- 현재 보고 있는 장소 자신(같은 id), 같은 주소, **직선거리 50m 이내**(기본값, `minDistanceMeters`로 조절 가능) 후보는 반드시 제외(`excludeNearSelf`) — 하버사인으로 정확히 계산.
- 각 후보에 상세 `categoryName`(카카오 원본 breadcrumb 전체)과 현재 장소 기준 `distanceMeters`를 붙여서 반환.

**AI 입력·프롬프트 (`src/app/api/ai-recommend/route.ts`)**

- candidates 에 `categoryName`, `lat/lng`, `distanceMeters`를 명시적으로 포함해서 전달(기존엔 이름·카테고리·주소만 줬음).
- 평가 기준을 프롬프트에 가중치로 명시: 취향·태그·분위기 유사도 45% / 데이트 맥락 25% / 후보 구체성·카테고리 적합성 20% / 거리 10% — "거리만으로 순위를 매기지 마세요"를 명시.
- "정확히 N개"가 아니라 "최대 N개, 부족하면 억지로 채우지 말 것(0개도 가능)"으로 변경.
- "candidates 에 없는 메뉴·분위기·영업 특성을 지어내지 말 것", "가까워서 편합니다류 반복 금지", "고른 항목끼리 다양하게" 지시 추가.

**검증 (실제 API 반복 호출 전, 요청대로 고정 데이터로 먼저 확인)**

1. 순수 로직(네트워크 없음, `tsx`로 즉석 스크립트 실행 후 삭제): 자기 자신 id 제외 / 같은 주소 제외 / 50m 이내 제외 / 300m 통과 — 5개 케이스 전부 통과.
2. 고정 후보 데이터로 `/api/ai-recommend` 단독 호출(진짜 Kakao 검색 없이): 60m·90m 거리의 무관한 후보(부동산·편의점)는 버리고 900m·450m 거리의 컨셉이 맞는 후보(북카페·와인바)만 선택 — 거리보다 취향 유사도 우선 확인. reason 문장도 주어진 category/address/distance 만 근거로 작성됨(메뉴·분위기 지어내지 않음).
3. 전부 무관한 후보(세탁소·약국·부동산·철물점)만 준 경우 0개 반환 — 억지로 3개 채우지 않음 확인.
4. 실제 Kakao API 1회 호출로 다중 검색어·중복제거·거리 필터 동작 확인: 최소 거리 56m(50m 컷 바로 위), 분식/한식/카페/술집 등 카테고리 다양성 확인.
5. 실제 저장된 장소("코오모라멘")에서 최종 UI 재검증: 80~150m 거리의 실제 후보 3개가 "담백한 라멘을 좋아하는 사람에게는... 국수 요리라는 공통점" 같은 구체적 근거로 표시됨. UI(카드 디자인, 버튼)는 변경하지 않음 — 요청대로.

9. 사용자 검증 후 코스 작성 화면에 1개 장소부터 추천을 연결한다. — 완료.
   - `src/components/CourseForm.tsx`에 "AI 추천 — 다음은 어디로 갈까요?" 섹션 추가. 코스에 장소가 **1개 이상** 담기면 나타남, 기본 접힘.
   - 기준점은 **코스의 마지막 정거장**(§5 "마지막 장소... 기준으로 거리 점수 반영"). 검색 반경은 기존 "이 근처" 로직과 같은 2km(`NEAR_KM`).
   - `/api/ai-recommend`에 `mode: "course"` 추가(장소 상세와 다른 프롬프트): **동선·거리 50% / 코스 전체 컨셉 30% / 후보 적합성 20%** — 장소 상세(거리 10%)와 반대로 거리를 최우선 기준으로 명시. `courseStops`(코스 전체, 순서대로)를 함께 전달해 공통 분위기·카테고리 조합을 반영.
   - PICK_COLUMNS 에 `tags` 추가(코스 정거장의 태그를 검색어·AI 컨텍스트에 사용).
   - 카드에 "+ 코스에 추가" 버튼 — 클릭하면 기존 "새 장소 추가 → 이번 코스에만 추가"와 동일한 경로로 `places`에 `course_only` 상태로 저장한 뒤 코스 순서에 바로 담는다(추가된 후보는 목록에서 사라져 중복 클릭 방지).
   - 실제 코스 작성 화면에서 검증: "코오모라멘"을 1번 정거장으로 넣자마자 섹션이 나타남 → 펼치면 실제 근처 후보(약수순대국 76m, 장다리곱창 약수점 102m, 소담촌 약수직영점 280m)가 "후보 중 가장 가까워 동선 부담이 적습니다", "기존 라멘·한식 식사와 겹치지 않는 샤브샤브 카테고리" 등 거리+다양성 근거로 표시됨 → "+ 코스에 추가" 클릭 시 정거장 2번으로 정상 추가되고 추천 목록에서 사라짐.
   - **주의**: 위 검증 중 "약수순대국"이 실제로 `places`에 `course_only`로 저장됐고, 테스트 코스 자체는 저장하지 않고 나가서 이 장소는 어느 코스에도 연결 안 된 채(owning_course_id: null) 남아있음. 화면 어디에도 노출되지 않아(course_only는 모든 목록에서 제외) 무해하지만, 정리하고 싶으면 Supabase SQL Editor에서 `delete from public.places where name = '약수순대국' and status = 'course_only' and owning_course_id is null;` 실행하면 됨(Claude가 직접 실행하지 않음).
10. 비용 제한, 오류/빈 상태, 최대 5개 더보기, 추천 새로고침을 검증한다. — 완료.
    - `/api/ai-recommend`에 비용 방어(최소 버전) 추가: 커플당 **시간당 최대 20회** 인메모리 rate limit(`checkRateLimit`), 초과 시 429 + 안내 메시지. **한계**: 서버 인스턴스 메모리 기반이라 Vercel 서버리스처럼 인스턴스가 여러 개거나 재시작되면 완벽히 막지는 못하는 best-effort 안전판(코드 주석에도 명시). 실제로 문제가 될 만큼 트래픽이 커지면 DB 기반(호출 로그 테이블)으로 교체 필요.
    - 순수 로직 테스트(네트워크 없음): 20회까지 허용·21회부터 차단, 커플별 독립 카운트, 1시간 지나면 재허용 — 3개 케이스 전부 통과.
    - 오류 상태: `/api/ai-recommend`·`/api/kakao-candidates`에 필수 필드 누락 요청 → 실제 호출 전 400 + 정확한 한국어 에러 메시지 확인(OpenAI/Kakao 호출 전 검증이라 비용 발생 없음). 비로그인 요청은 여전히 `/login`으로 리다이렉트됨(회귀 확인).
    - 최대 5개/새로고침: `count: 999`로 요청해도 서버가 5로 clamp하고, AI는 억지로 채우지 않고 실제로 맞는 만큼(3개)만 반환 — §8.1에서 검증한 "억지로 채우지 않음" 정책이 극단값에서도 유지됨을 재확인.
    - 빈 상태는 §8.1에서 이미 검증(무관한 후보만 있을 때 0개 반환 + UI 안내 문구).
11. 사용자가 명시적으로 승인하기 전에는 운영 DB 적용, commit, push, Vercel 배포를 하지 않는다.

## 12. 완료 기준

- AI가 존재하지 않는 장소를 직접 만들어 추천하지 않는다.
- Kakao 후보 중 실제 장소만 결과로 반환한다.
- 기본 결과 3개, 추가 결과 포함 최대 5개다.
- 장소 상세 추천은 서울·경기 범위에서 취향 유사도를 우선한다.
- 코스 추천은 선택 장소가 1개일 때부터 작동하고 가까운 동선을 우선한다.
- 추천 이유가 해당 장소·태그·코스 맥락을 구체적으로 설명한다.
- date.log 기존 사진이 있으면 표시하고 없으면 이모지와 네이버 이미지 검색 링크가 작동한다.
- 모바일과 데스크톱에서 카드가 잘리지 않는다.
- OpenAI 키가 클라이언트 번들에 포함되지 않는다.
- API 오류나 예산 제한 시 빈 카드 대신 이해 가능한 안내를 보여준다.


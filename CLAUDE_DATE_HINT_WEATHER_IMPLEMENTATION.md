# date.log 데이트 힌트 · 날씨 기록 UI 구현 지시서

아래 요구사항에 따라 현재 date.log 코드베이스에 기능을 구현해 주세요. 디자인을 새로 탐색하거나 전면 재설계하지 말고, 이미 확정된 미리보기 UI와 현재 프로젝트의 디자인 토큰을 기준으로 실제 기능에 연결합니다.

## 0. 작업 전 필수 확인

1. 루트의 `AGENTS.md`(=`CLAUDE.md`)와 `HANDOFF.md`를 먼저 끝까지 읽고 현재 코드와 대조합니다.
2. 이 프로젝트의 Next.js 버전은 기존 지식과 다를 수 있으므로 수정할 기능에 해당하는 `node_modules/next/dist/docs/` 문서를 먼저 확인합니다.
3. 기존 사용자 변경과 무관한 파일은 수정하지 않습니다.
4. 운영 SQL 실행, 운영 데이터 생성·수정, commit, push, 배포는 하지 않습니다. 필요한 DB 변경은 migration/SQL 파일만 작성하고 사용자 승인 전에는 실행하지 않습니다.
5. 비밀키와 사용자 개인정보를 코드·로그·문서에 남기지 않습니다. 날씨 API 키는 서버 환경변수만 사용합니다.
6. 현재 working tree에는 다른 작업에서 만든 `/api/weather`, `/api/special-days` 또는 관련 코드가 이미 있을 수 있습니다. 먼저 실제 구현과 diff를 확인하고, 작동하는 코드를 중복 생성하거나 덮어쓰지 말고 아래 확정 UI에 연결합니다.

## 1. 확정된 제품 원칙

날씨는 사용자를 매일 방해하는 추천 팝업이 아니라, **그날의 기억을 더 선명하게 남기는 기록 메타데이터**로 사용합니다.

- 팝업: 절기·기념일·공휴일 등 빈도가 낮고 의미 있는 날에만 사용
- 일반 날씨: 팝업으로 매일 띄우지 않고 홈 제목 옆의 작은 날씨 버튼으로 표시
- 추억: 저장 순간의 확인 가능한 현재 날씨를 함께 기록
- 회고: 저장된 날씨를 `그날도 비가 왔었어요` 같은 문장에 활용
- 날씨별 추억 필터: 구현하지 않음
- 확인하지 못한 과거 날씨나 기온: 추정하거나 자동 생성하지 않음

## 2. 디자인 기준

현재 date.log 디자인 시스템을 그대로 사용합니다.

- Archive Teal 중심색과 기존 CSS 변수 사용
- Warm cream 배경, white/ivory card, hairline border
- 카드 radius 20px, 팝업 radius 28px 유지
- 본문과 기능 UI는 기존 Pretendard 계열
- 메타데이터는 작고 절제된 크기
- 이모지는 컬러 장식이 아니라 날씨를 빠르게 인식시키는 작은 기호로만 사용
- 새로운 그림자·그라디언트·강한 색상·대형 일러스트를 추가하지 않음
- 모바일 우선, 데스크톱에서도 현재 max-width 체계 유지

## 3. 데이트 힌트 팝업

### 3-1. 확정 UI

기존 `src/components/DateSuggestionPopup.tsx`의 UI를 최종안으로 사용합니다.

- Archive Teal 상단선
- Ivory surface
- 작은 경로 모티프
- eyebrow → 짧은 제목 → 감성 문장 → 보조 메타 → primary CTA
- `다음에 볼게요`와 X 닫기
- dialog semantics, focus trap, Escape 닫기, body scroll lock 유지
- 한 팝업에 primary CTA는 하나만 노출

현재 미리보기:

- `/?datePopup=season`: 입춘
- `/?datePopup=christmas`: 크리스마스
- `/?datePopup=preview`: 일반 맑음 샘플. 일반 날씨의 실제 팝업 트리거로 사용하지 말 것

### 3-2. 노출 정책

- 실제 팝업 대상: 절기, 커플 기념일, 생일, 공휴일 및 특별한 시즌 이벤트
- 일반적인 맑음·비·기온 변화는 팝업 대상에서 제외
- 같은 이벤트는 사용자별로 하루 최대 한 번만 표시
- 사용자가 닫으면 그날 다시 표시하지 않음
- 여러 이벤트가 겹칠 때 한 번에 하나만 표시하도록 명시적인 우선순위를 둠
- 팝업 조건 계산 중이거나 오류가 나면 팝업을 띄우지 않음. 로그인 직후 화면을 막지 말 것

권장 우선순위:

1. 사용자/커플 기념일 및 생일
2. 공휴일·문화 기념일
3. 절기·특별한 계절 이벤트

### 3-3. CTA 연결 원칙

팝업 CTA는 모든 이벤트를 `/wishlist`로 보내지 않습니다.

- 기념일·생일: 과거 같은 기념일에 방문한 장소나 추억 모음
- 크리스마스 등 반복 기념일: 지난 같은 날의 `○○에 다녀왔어요` 태그가 붙은 장소
- 1년 전 오늘: 해당 추억 또는 해당 날짜 기록
- 절기: 문맥과 데이터에 따라 관련 위시리스트 또는 다녀온 장소

사용자 기록이 실제로 존재할 때만 `지난 생일 돌아보기`, `지난 크리스마스 돌아보기` 같은 회고형 CTA를 사용합니다. 과거 기록이 없다면 `함께 갈 곳 찾아보기`처럼 위시리스트/관련 카테고리 CTA로 fallback 합니다. 존재하지 않는 장소 수나 과거 기록을 문구에 만들지 않습니다.

기념일 태그와 일치하는 결과 화면에는 이미 구현된 `○○에 다녀왔어요` 표현을 재사용하고, 새로운 중복 태그 체계를 만들지 않습니다.

## 4. 설정 화면

`src/components/SettingsView.tsx`의 기존 구조와 어울리는 설정 섹션을 추가합니다.

### 기본 노출

- 섹션명: `데이트 힌트`
- 설명: `데이트하기 좋은 특별한 날에 둘만의 제안을 보여드려요.`
- 전체 ON/OFF 스위치: `데이트 힌트 받기`

### 세부 설정

기본 화면을 복잡하게 만들지 않도록 아코디언/접기 영역에 둡니다.

- `기념일과 특별한 날`
- `절기와 계절`
- `공휴일과 시즌`

일반 날씨 팝업 설정은 만들지 않습니다. 날씨는 홈 위젯이므로 데이트 힌트 팝업 설정과 분리합니다.

설정은 사용자 개인 preference로 저장되어야 합니다. 커플 한 명이 끄더라도 상대방 설정을 변경하지 않습니다. DB 스키마가 필요하면 migration 파일과 타입 변경을 작성하되 운영에 실행하지 않습니다.

## 5. 홈 날씨 위젯

최신 확정 시안은 `src/components/HomeWeatherWidget.tsx`와 `/?weatherWidget=preview`입니다. 이전의 독립 카드/긴 pill 시안으로 되돌리지 않습니다.

홈의 `우리가 다녀온 곳` 제목 오른쪽에 배경과 테두리가 없는 작은 날씨 버튼으로 배치합니다. 접힌 상태에서는 날씨 아이콘과 현재 기온만 보여줍니다.

예시:

```text
우리가 다녀온 곳                         ☀ 22°
지금까지 46곳 · 함께 만든 추억 13개
```

요구사항:

- 접힌 버튼에는 `☀ 22°`처럼 아이콘과 기온만 표시
- `서울`, `맑음`, 감성 문구는 접힌 상태에 노출하지 않음
- 배경색·테두리·긴 pill을 사용하지 않음. 아래 피드/지도/검색/카테고리 조작부와 시각적으로 경쟁하지 않게 함
- 누르면 제목 아래에 레이아웃 이동 없이 작은 팝오버를 표시
- 팝오버에는 `지역`, `날씨 상태`, `현재 기온`, `체감온도`, `최고/최저기온`, `강수확률`, `관측 기준 시각`, 짧은 사실 기반 문구만 표시
- 팝오버는 바깥 클릭과 Escape로 닫힘
- 전체 화면 modal, 큰 CTA, 닫기 X, carousel로 만들지 않음
- 위치·날씨를 불러오는 동안 레이아웃이 크게 움직이지 않게 함
- 위치 권한이 없거나 API 실패 시 거슬리는 오류 카드 대신 버튼을 숨기거나 중립적인 fallback 사용
- 현재 위치의 정밀 주소를 화면이나 로그에 노출하지 않음. `서울` 정도의 지역 단위만 표시
- 상세 문구는 짧고 사실에 기반하며, 지나치게 적극적인 추천 문구를 만들지 않음
- 날씨 정보에는 갱신 시각/캐시 정책을 정하고 API를 매 렌더마다 호출하지 않음
- 버튼에는 화면 낭독기용으로 `서울 오늘 날씨 맑음 22도. 자세히 보기`처럼 완전한 accessible label 제공
- 모바일에서 제목과 `☀ 22°`가 한 줄에 안정적으로 놓이는지 확인하고, 긴 제목에서도 겹치지 않게 함

## 6. 추억 저장 시 날씨 기록

기존 `src/components/AddMemoryForm.tsx`에 확정 시안의 `그날의 날씨` UI를 연결합니다.

### 오늘 날짜

- 현재 날씨를 자동으로 제안
- 표시 예: `☂ 비 · 19°` / `서울 · 자동 확인`
- 안내: `오늘 날씨를 자동으로 가져왔어요. 다르게 기억한다면 바꿔주세요.`
- 사용자가 `맑음 / 흐림 / 비 / 눈` 중 직접 수정 가능
- 사용자가 수정하면 수동 선택값을 최종 저장

### 과거 날짜

- 무료 현재 날씨 API 값으로 과거 날씨를 채우지 않음
- `그날 날씨 직접 선택` UI만 표시
- 사용자가 `맑음 / 흐림 / 비 / 눈`을 선택할 수 있음
- 과거 기온을 확인할 수 없다면 `temperature`는 null로 저장
- 아무것도 선택하지 않고 저장하는 것도 허용. 날씨는 추억 작성의 필수값이 아님

### 사진 EXIF로 날짜가 바뀌는 경우

- 기존 촬영일 자동 입력 동작 이후 날짜가 오늘인지 다시 판정
- 과거 촬영일이면 방금 받은 현재 날씨를 자동 부착하지 않음
- 날짜를 오늘→과거로 바꾸면 자동 날씨를 지우고 수동 선택 상태로 전환
- 날짜를 과거→오늘로 바꾼 경우에만 현재 날씨를 다시 제안할 수 있음

### 수정 화면

- 기존 추억을 수정할 때 저장되어 있던 날씨를 유지
- 날짜만 수정했다고 기존 날씨를 조용히 덮어쓰지 않음
- 날짜와 날씨의 불일치 가능성을 사용자에게 짧게 알리고 직접 선택하게 함

## 7. 날씨 데이터

정확한 기존 DB 구조를 먼저 확인하고 최소 변경으로 설계합니다. 권장 필드는 다음과 같지만, 현재 스키마와 충돌하면 더 적절한 명칭을 사용하고 이유를 기록합니다.

- `weather_condition`: nullable enum/text (`clear`, `cloudy`, `rain`, `snow`)
- `temperature_c`: nullable numeric
- `weather_location`: nullable text. 정밀 좌표가 아니라 화면 표시용 광역 지역
- `weather_observed_at`: nullable timestamptz
- `weather_source`: nullable text (`api`, `manual`)

규칙:

- 자동 기록은 API 응답 성공 시에만
- 수동 기록은 condition만 필수이며 temperature는 null 가능
- 저장 시점의 날씨를 영구 스냅샷으로 보관하며 나중에 현재 날씨로 갱신하지 않음
- 기존 추억에는 null을 유지하고 소급 생성하지 않음
- 날씨 조회 실패 때문에 추억 저장 자체가 실패하면 안 됨
- 위치정보 최소 수집 원칙을 지키고, 좌표 저장이 필요하지 않다면 저장하지 않음

DB 변경이 필요하면 migration과 rollback/검증 방법을 제시하되 운영 SQL을 실행하지 않습니다. 생성한 정책이 커플 격리 RLS를 우회하지 않는지 확인합니다.

## 8. 추억 카드와 회고

### 추억 카드

`src/components/MemoriesFeed.tsx`에서 날씨가 있는 기록에만 날짜 근처에 작게 표시합니다.

예시:

```text
2026.09.06 · 서울   ☂ 비 19°
```

- 날씨가 null인 기존 추억은 빈 공간이나 placeholder 없이 기존 카드 그대로 표시
- 날씨가 본문·사진·장소보다 시각적으로 강해지지 않게 함
- 현재 카드의 메타데이터 위계와 정렬을 유지

### 회고

`작년 이맘때` 등 기존 회고 UI에서 실제 저장 날씨가 있을 때만 다음과 같은 보조 문장을 사용할 수 있습니다.

```text
그날도 비가 왔었어요.
```

- 저장된 데이터가 없으면 문장을 표시하지 않음
- 날씨를 근거로 당시 감정이나 상황을 추정하지 않음
- 새로운 날씨별 검색/필터/탭은 만들지 않음

## 9. 명시적 제외 범위

다음 항목은 구현하지 않습니다.

- 매일 날씨 팝업
- 날씨별 추억 필터 (`비 오던 날의 기록` 필터 포함)
- 과거 기록의 날씨 소급 채우기
- 현재 날씨를 과거 날짜의 날씨로 저장
- 확인되지 않은 과거 기온 생성
- 날씨 기반 AI 데이트 추천 확대
- 새로운 대형 날씨 페이지
- 팝업 안에 여러 CTA 배치
- 운영 SQL 실행, commit, push, 배포

## 10. 미리보기 코드 처리

- 홈 날씨의 최종 기준은 `HomeWeatherWidget.tsx`와 `/?weatherWidget=preview`입니다. `/auth/weather-archive-qa`에서는 같은 실제 컴포넌트를 사용해야 합니다.
- `/auth/weather-archive-qa`의 날씨 필터 칩은 폐기된 비교 시안으로 실제 구현 대상이 아닙니다.
- 실제 컴포넌트 구현 후에도 QA에 가치가 있으면 실제 컴포넌트를 sample props로 렌더하도록 바꿉니다.
- 단순 복제 코드가 되어 유지보수 부담만 남는다면 실제 화면 검증 후 제거 여부를 사용자에게 보고합니다. 임의로 삭제하지 않습니다.
- `HomeView.tsx`의 `datePopup` query sample은 UI 검토용입니다. 실제 트리거와 혼동되지 않도록 개발 전용 여부를 명확히 유지합니다.

## 11. 접근성 및 상태 처리

- 모든 스위치와 날씨 선택 버튼에 label 및 `aria-pressed`/적절한 semantics 제공
- 키보드만으로 팝업 닫기와 설정 변경 가능
- loading, API 실패, 위치 권한 거부, 날씨 없음, 기존 추억 수정 상태를 처리
- 색상만으로 날씨 선택 상태를 전달하지 않음
- `prefers-reduced-motion` 존중
- 모바일 320px 너비에서 텍스트·버튼이 잘리지 않게 확인

## 12. 완료 조건

구현 후 아래를 확인하고 `HANDOFF.md`에 검증 결과와 미검증 사항을 업데이트합니다.

1. 데이트 힌트 전체 OFF 사용자는 팝업을 보지 않음
2. 설정은 상대방이 아닌 로그인 사용자에게만 적용됨
3. 일반 날씨로는 팝업이 열리지 않음
4. 특별한 이벤트 팝업은 하루 한 번만 노출되고 dismiss가 유지됨
5. 과거 같은 기념일 기록이 있으면 해당 태그의 장소/추억으로 이동
6. 기록이 없으면 거짓 카운트 없이 안전한 fallback CTA 표시
7. 홈 제목 오른쪽에 `날씨 아이콘 + 기온`만 표시되고, 클릭 팝오버가 열리며 실패 시 홈 사용을 방해하지 않음
8. 오늘 날짜 추억에는 현재 날씨가 제안되며 사용자가 수정 가능
9. 과거 날짜에는 현재 날씨/기온이 자동 저장되지 않음
10. 날씨 API 실패 시에도 추억 저장 성공
11. 기존 날씨 null 추억의 표시와 수정이 깨지지 않음
12. 날씨 필터가 새로 생기지 않음
13. 모바일·데스크톱 레이아웃 확인
14. 타입 검사, 수정 파일 lint, `git diff --check`, production build 실행

최종 보고에는 다음을 구분해 적어 주세요.

- 실제 구현한 항목
- 작성했지만 운영에 적용하지 않은 migration/SQL
- 로컬에서 검증한 항목
- API 키·실기기·운영 DB 때문에 검증하지 못한 항목
- 사용자 승인이 필요한 다음 단계

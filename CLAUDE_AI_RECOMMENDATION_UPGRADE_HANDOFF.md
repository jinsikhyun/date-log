# date.log AI 추천 고도화 — Claude Code 인수인계

최종 상태 반영: 2026-09-10
상태: 0·3·4·5단계 구현 및 검증 완료. 1·2·6·7단계는 아래 계획만 있으며 구현 완료로 간주하지 않는다.

## 1. 목적과 작업 방식

핵심 가치: **기록이 쌓일수록 추천이 우리다워진다.** 개인의 취향과 두 사람의 공통점·차이를 실제 기록에 근거해 반영한다. 신규 사용자는 혼자서도 시작하고, 짧은 설문과 최소 기록으로 첫 추천을 받을 수 있어야 한다.

- Claude Code: 현재 구조 확인, 데이터·추천 로직·백엔드·실제 화면 연결과 검증을 담당한다.
- Codex와 사용자: 간단한 취향 설문 UI 미리보기를 만들고 피드백으로 디자인을 확정한다. Claude는 별도 요청 없이 설문 시각 디자인을 확정하거나 전면 개편하지 않는다.
- 단계별로 비교 가능한 결과를 남긴다. 추천 개선 효과는 가설이며 실제 평가 전에는 성공률·효과 크기를 단정하지 않는다.
- 시작 시 `AGENTS.md`, `HANDOFF.md` 최상단, 이 문서를 읽고 현재 코드와 대조한다. Next.js 수정 시 저장된 버전의 로컬 문서를 따른다.
- `AI_RECOMMENDATION_HANDOFF.md`는 초기 구현 이력이 포함된 참고 문서다. 과거 완료/미구현 설명과 모델 가격을 현재 사실로 그대로 재사용하지 않는다.
- 운영 SQL·실데이터 변경·commit/push/배포는 사용자 승인 범위를 확인해 진행한다. 로컬 구현·가상 데이터 검증 결과를 먼저 제시한다.

## 2. 2026-09-10 현재 구현 상태

아래 상태는 현재 코드, `HANDOFF.md`, 최근 커밋을 대조한 결과다. 세부 실측과 트레이드오프는 `HANDOFF.md` 최상단의 AI 추천 절을 우선한다.

| 단계/항목 | 현재 상태 | 남은 작업 |
| --- | --- | --- |
| 0단계 비교 기준선 | 완료. 가상 fixture·평가 스크립트와 반복 비교 기준 마련 | 신규 정책 변경 때 같은 fixture로 회귀 비교 |
| 1단계 혼자 시작 | 미구현 | solo 소유·파트너 연결 전환 모델부터 설계 |
| 2단계 취향 설문 | 미구현 | UI 확정 후 개인별 저장 계약과 추천 입력 연결 |
| 3단계 후보 수집 | 완료. 구체 검색어 파생, 거리/업종 다양화, 출처 메타 보존, `allocateBySourceKind` 배분 정책 연결 | 표본 확대 후 `MIN_SPECIFIC_SLOTS=10` 재평가 |
| 4단계 취향 프로필 | 완료. `buildTasteProfile`이 장소 ID 기준으로 pick·위시·공동 별점·단골·감정·제한된 추억 원문을 묶어 실제 API route에 연결 | 실사용 기록 분포에서 상한·반감기·근거 충분도 재평가 |
| 5단계 근거/위시 활용 | 완료. 프롬프트의 사실성·상충 근거 보존과 코스 추천의 위시 별도 후보 주입·중복 방지·출처 표시 연결 | `WISH_MAX_SLOTS=2`의 실사용 적정성 관찰 |
| 6단계 관심 없어요 | 미구현 | 개인 귀속, 취소, 사유별 의미, 재노출 기간 정책 확정 후 구현 |
| 7단계 후속 실험 | 미구현 | 앞 단계 데이터가 쌓인 뒤 검토 |
| 상세 장소 추천 | 동작 중. 저장 장소 제외 후 새 후보를 추천하고 근거를 표시 | 5단계 위시 직접 후보 주입은 코스 모드에만 적용(의도된 범위) |
| 코스 추천 | 동작 중. 전체 코스 맥락·일시적 조건·우리 위시 후보를 반영 | 후보 슬롯 트레이드오프 지속 관찰 |
| 보안/비용 | 인증·커플 범위 검사, DB 기반 rate limit 경로 존재 | 운영 적용 상태와 실제 호출 비용은 환경별로 별도 확인 |

주요 파일:

- `src/app/api/ai-recommend/route.ts`
- `src/app/api/kakao-candidates/route.ts`
- `src/lib/recommendationPolicy.ts`
- `src/lib/recommendationFeedback.ts`
- `src/lib/recommendationVoice.ts`
- `src/lib/kakaoLocal.ts`
- `src/components/AiRecommendationSection.tsx`
- `src/components/CourseForm.tsx`

## 3. 구현 순서와 단계별 완료 기준

### 0단계 — 비교 기준선 확보

현재 추천을 재현할 수 있는 가상 사용자·후보 fixture와 평가표를 먼저 만든다.

- 기록 없음, 한 사람만 기록 있음, 취향 충돌, 높은 별점과 부정 감정 충돌, 오래된 단골, 같은 업종 반복, 적합한 먼 후보, 부적합한 가까운 후보를 포함한다.
- 기존 후보 수집과 추천 입력을 별도로 기록해 어느 단계에서 문제가 생기는지 구분한다.
- 원문·식별정보를 무제한 운영 로그에 남기지 않는다. 유료 실제 호출은 필요한 비교 범위를 정해서 실행한다.
- 완료: 같은 입력으로 변경 전후를 비교할 방법이 있고 현재 품질의 약점이 기록됨.

### 1단계 — 혼자 시작 및 파트너 연결

가입자가 파트너 연결을 기다리지 않고 기록·위시·추천을 시작하도록 한다.

- 기존 인증·소유 범위를 검토해 1인 기록 공간을 기존 구조 안에서 표현할지, 개인 소유 모델이 필요한지 결정한다. 단순히 couple_id 검사를 제거하지 않는다.
- 개인 취향 답변은 개인에게 귀속되며 파트너 연결 이후에도 유지한다.
- 양쪽 모두 기존 기록이 있을 때의 연결·중복·소유권 처리와 공유 범위를 먼저 명시한다. 연결만으로 상대에게 어떤 기존 기록이 보이는지 사용자에게 명확히 한다.
- 혼자일 때와 연결됐을 때의 문구를 구분한다. 파트너 응답이 없으면 공통 취향을 만들어내지 않는다.
- 완료: solo 시작→기록→추천→파트너 연결에서 기록과 개인 선호가 보존되고 접근 범위가 의도대로 유지됨.

### 2단계 — 설문 저장과 신규 추천 연결

설문 UI는 Codex/사용자가 별도로 확정한다. Claude는 먼저 선택값·저장 형식·완료 상태의 연결 계약을 정한다.

- 개인별 답변, 설문 버전, 선택값, 건너뜀/일부 응답/완료를 구분한다.
- 건너뛰기는 무관심이나 불호가 아니다. 설정에서 재응답·수정 가능해야 한다.
- 업종 선택은 실제 후보 검색에 연결한다. 후보가 없는 선택을 조용히 무시하지 말고 결과 부족 상태를 처리한다.
- 기록이 적으면 설문·위시를 활용하고, 평가 기록이 쌓이면 실제 경험 근거를 더 반영한다. 현재 요청의 명시적 조건은 평소 선호보다 우선한다.
- 추천 지역은 별도의 현재 맥락이다. 기준 장소도 위치도 없는 신규 사용자에게는 추천 시 지역 선택 등 최소 기준을 제공한다. 설문을 취향 위치 수집으로 늘리지 않는다.
- 완료: 답변 변경이 실제 후보/평가에 반영되고, 건너뛴 사용자도 첫 추천에 도달함.

### 3단계 — 후보 수집 개선

- 상세 추천은 기준 장소와 취향이 연결되는 대안을 찾고, 코스 추천은 전체 활동 구성과 이동을 고려한다.
- 서버에서 사용자가 확인한 태그와 관련 기록을 조회한다. 클라이언트의 과거 `tags` 배열을 그대로 확정 취향으로 쓰지 않는다.
- 검색어에 활용 가능한 구체 개념과 후보 특성으로 확인되지 않은 분위기 희망을 구분한다.
- 검색어·정렬 방식·원래 순위 등 후보 출처를 보존한다. 여러 API 목록을 합친 순서를 전체 정확도순이라고 간주하지 않는다.
- 상세 후보는 거리대와 관련 업종을 분산한다. 먼 장소 수를 억지로 채우지 않으며 적합성 부족 시 적은 결과를 허용한다.
- 멀리 있는 후보가 원래 조회 결과에 없다면 후처리 배분만으로 생성되지 않는다. 검색어/검색 지역별 확보 상태도 확인한다.
- 코스의 근거리 정책은 회귀 검증한다. 후보 목록 순서 실험은 재현 가능하게 수행한다.
- 완료: 관련 있는 먼 후보가 최종 후보에 남으며, 거리 분산을 위해 무관한 후보를 늘리지 않음.

### 4단계 — buildTasteProfile

네트워크와 분리된 순수 함수로 만들고 현재 시각도 입력받아 검증 가능하게 한다.

대표 근거는 장소 ID를 중심으로 묶는다:

- 같은/관련 업종의 고평가 장소와 한줄평
- 개인별 pick, 둘 다 pick, 단골
- 공동 별점과 개인별 감정
- 사용자 확인 태그
- 위시: 방문 만족과 별도로 구분
- 추억 수와 추천에 관련 있는 짧은 원문
- 최근성 및 활동 지역 분포

필요한 출력 개념: 대표 장소, 관련 고평가 장소, 개인별 선호, 공통 선호, 부정 평가 근거, 위시 기반 지향, 근거의 충분성. 필드명과 상한은 Claude가 코드에 맞게 결정한다.

- 같은 장소가 여러 목록에 있다고 독립 근거로 중복 가산하지 않는다.
- 단골은 사용자가 단골로 지정한 사실이다. 실제 방문 횟수로 주장하지 않는다.
- 추억 개수는 만족·재방문 횟수가 아니다. 부정 감정과 충돌하면 선호로 승격하지 않는다.
- 낮은 별점은 해당 장소에 관한 평가다. 원인 없이 업종 전체를 불호로 일반화하지 않는다.
- 원문은 관련 있는 기록만 선별하고 부정·예외·작성자 맥락을 보존한다. 너무 긴 문장 절단으로 의미가 뒤집히지 않게 한다.
- 최근성은 오래된 단골을 자동 무효화하지 않는 보조 신호다. 생성일과 방문일을 혼동하지 않는다.
- `first_visit_date`만으로 시간대·날씨·야외 좌석 선호를 추론하지 않는다.
- 기록 충분성은 단순 개수뿐 아니라 평가의 구체성과 구성원별 근거 유무를 고려한다. 0–2/3–9/10+ 등의 경계와 반감기 180일은 확정값이 아닌 실험값이다.
- 완료: 충돌·희소성·개인별 기록량 차이에도 출처와 의미가 보존되는 테스트 통과.

### 5단계 — 근거 기반 추천 및 우리 위시 활용

- 프로필에서 확인한 사실과 후보에서 확인한 사실을 연결해 이유를 만든다.
- 충분한 근거가 있을 때 서로 다른 근거 2개를 목표로 한다. 부족하면 1개 또는 후보 사실만 사용한다. 거리는 취향 근거 개수에 넣지 않는다.
- 반환 근거 ID는 입력에 포함된 기록인지 서버에서 검증한다. ID 존재 검증만으로 문장 내용까지 사실임이 보장되지는 않으므로 문구 평가도 수행한다.
- 사용자 취향에 '담백한 라멘'이 있다고 후보의 국물이 담백하다고 주장하지 않는다. 메뉴·좌석·조용함은 후보 쪽 증거가 필요하다.
- 모델이 자체 출력한 confidence를 검증된 확률처럼 표시하지 않는다. 사용자에게는 필요한 경우 실제 근거를 보여준다.
- 코스 구성에는 우리 위시리스트를 별도 후보로 포함한다. 현재 전체 저장 장소 제외 로직을 목적별로 조정한다.
- 새로운 발견과 재방문 추천은 구분하고, 이미 아는 장소를 새로 발견한 것처럼 표시하지 않는다.
- 완료: 실제 기록과 후보 사실에 근거하며, 위시 후보 선택·코스 추가에서 중복과 출처가 정확함.

### 6단계 — 관심 없어요

- 기록할 최소 정보: 사용자, 추천 요청/노출 후보, 선택한 피드백, 발생 시각, 추천 모드. 개인별로 귀속한다.
- 관심 없음은 해당 후보의 재노출을 줄이는 데 먼저 활용한다. 취소 가능하게 한다.
- 선택적 이유 초안: 너무 멀어요 / 취향이 아니에요 / 이미 알아요.
- 미저장·미클릭은 거절로 저장하지 않는다. '이미 알아요'는 불호가 아니다.
- '너무 멀어요'는 해당 요청 조건일 수 있으므로 개인의 영구 불호로 즉시 확정하지 않는다.
- 한 사람의 거절로 상대방 선호를 지우지 않는다. 재노출 제한 기간은 구현 전 정책으로 명시한다.
- 완료: 개인 귀속·취소·사유별 처리·재노출 제어 검증.

### 7단계 — 후속 실험

- 의도 칩은 앞선 개선 후에도 질문의 모호함이 남을 때 검토한다.
- 분위기 선택 시 업종을 완전히 버리지 않는다. 근거가 있는 인접 업종으로 확장한다.
- 후보의 가격·혼잡·대기·공간 정보 보강은 별도 프로젝트로 검토한다.
- 탐색은 위시/명시적 관심 등 근거에 기반한 0~1개 슬롯부터 실험한다. 자료 부족 시 억지로 추가하지 않는다.
- 타 커플 기록 집계는 후순위다. 참여 동의, 동일 장소 매칭, 소수 응답 보호, 원문 비공개를 갖춘 장소 단위 통계부터 설계한다.

## 4. 설문 UI 연결 계약 초안

확정 방향: 가입 직후, 약 20초 목표, 한 화면 한 질문, 최대 3단계, 큰 칩, 텍스트 입력 없음, 뒤로/나중에 하기 제공. 20초는 실측 목표이며 보장 문구가 아니다.

1. 좋아하는 구성: 맛집 / 카페 / 술집·바 / 전시·문화 / 산책·야외 — 최대 2개.
2. 분위기: 조용한 / 활기찬 / 감성적인 / 색다른 — 최대 2개. 서로 겹칠 수 있는 키워드이며 상호배타 분류가 아니다.
3. 피하고 싶은 조건: 긴 웨이팅 / 먼 이동 / 붐비는 곳 / 비싼 곳 / 딱히 없음 — 선택 개수는 UI 피드백에서 확정. '딱히 없음'은 다른 선택과 동시에 선택되지 않게 한다.

개인 응답 문구는 '데이트할 때 나는 이런 곳이 좋아요'처럼 자신에 대해 답하도록 한다. 파트너가 연결되고 실제 응답이 있을 때만 공통 선택을 보여준다. 공통 항목이 없으면 억지로 교집합을 생성하지 않는다.

불호·분위기는 희망 조건이다. 후보에 해당 정보가 없으면 충족/불충족을 확정하지 않는다. 정보 없는 후보를 전부 탈락시키지도 않는다. 특히 '웨이팅 없는 곳을 골랐어요'처럼 확인 능력을 넘어서는 문구를 쓰지 않는다.

`위시 1곳 + 방문 2곳`은 선택형 시작 가이드다. 첫 추천의 필수 조건으로 만들지 않는다. 먼저 좋아하는 장소 1곳(방문/위시 모두 허용)을 받아 첫 가치를 보여주고 추가 기록을 유도한다. 신규 사용자에게 별점을 필수로 요구하지 않되, 자발적인 평가를 일괄 무의미하게 취급하지 않는다.

## 5. 데이터 한계와 장기 방향

카카오 Local 키워드 검색의 문서화된 장소 응답: ID·이름·카테고리·전화번호·주소·좌표·장소 URL·중심까지 거리. 웨이팅·가격·혼잡·좌석·분위기 필드는 없다.

공식 출처(2026-09-08 확인): https://developers.kakao.com/docs/ko/local/dev-guide#search-by-keyword

- 먼 이동은 직선거리 기준으로 일부 반영 가능하다. 도보시간·실제 경로와 동일하지 않다.
- 사용자의 선호를 자세히 알아도 후보 특성의 빈칸이 자동으로 채워지지는 않는다.
- 우리 기록은 출처가 있는 주관적 경험이다. 100% 정확하거나 영구적으로 유효한 사실은 아니다.
- 우리 위시리스트의 기록 활용을 먼저 검증하고, 다른 커플의 집계는 사용자 기반과 근거가 쌓인 뒤 진행한다.
- 모델명·가격은 기존 문서의 수치를 확정 사실로 사용하지 않는다. 실제 환경의 모델과 최신 공식 요금, 입력/출력 사용량, 호출 수로 비용을 측정한다.

## 6. 효과 검증 및 보고 형식

후보 수집 변경과 취향 프로필 변경의 효과를 분리한다. 프로필 비교는 같은 후보 목록으로 실시하고, 후보 수집 비교는 같은 기준 장소/사용자 조건으로 실시한다.

| 평가 대상 | 확인할 것 |
| --- | --- |
| 신규 사용자 | 완료/건너뜀, 첫 추천까지 시간, 첫 저장, 단계별 이탈 |
| 후보 풀 | 적합 후보 포함 여부, 업종/거리 분포, 불필요한 후보 증가 여부 |
| 추천 결과 | 같은 조건에서 사용자가 더 선호하는 결과, 이유의 구체성 |
| 사실성 | 존재하는 기록 인용, 확인 안 된 후보 특성 단정 여부 |
| 커플 균형 | 기록량이 적은 사람의 명시적 취향도 반영되는지 |
| 운영 성능 | 응답 시간, 후보 검색 실패, 실제 토큰/호출 비용 |

평균 거리가 늘거나 문장이 길어진 것만으로 성공이라 판단하지 않는다. 소규모 사용성 검증은 방향 확인용이며 전체 사용자에 대한 통계적 효과로 주장하지 않는다.

각 단계 종료 보고: 변경 내용 / 비교 결과 / 실패·미검증 사항 / 필요한 SQL 또는 UI 연결 사항 / 다음 단계. 같은 체크가 통과했다면 이유 없이 반복하지 않는다.

## 7. 현재 저장소 상태 참고

`HANDOFF.md` 최상단에 2026-09-08 00:22:35 KST 사용자 제공 운영 조회 결과가 기록돼 있다. place-photos 과거 공개·익명 CRUD 문제는 그 설정·정책·함수 정의 검토 범위에서 해결 상태다. 과거 커밋의 미검증 기록을 현재 미해결 증거로 반복 사용하지 않는다. 새로운 상충 증거가 있을 때만 구체적으로 검토한다.

현재 사용자 소유 변경/비추적 파일을 보존한다. 이 문서는 개발 계획 인수인계이며 운영 변경 권한을 추가로 부여하지 않는다.

## Claude Code 다음 작업

0·3·4·5단계를 처음부터 다시 구현하지 않는다. 먼저 현재 코드와 `HANDOFF.md`의 최신 AI 추천 절을 대조하고 기존 테스트를 기준선으로 실행한다. 다음 기능 작업은 별도 사용자 지시가 없다면 6단계 `관심 없어요`이며, 구현 전에 개인 귀속·취소 UX·사유별 의미·재노출 제한 기간을 사용자와 확정한다. 1단계 solo 시작과 2단계 설문은 독립된 미완료 과제로 유지하며 임의로 함께 구현하지 않는다.
